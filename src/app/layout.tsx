import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Wellu Wellu Wellness Center',
  description: 'Providing easy access to trusted Natural (Non-Synthetic, Herbal, Botanical or Phytomedicines) products and scarce Drugs for maximal Health Care.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
